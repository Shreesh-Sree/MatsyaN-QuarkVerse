rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for testing - REPLACE WITH SECURE RULES IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
